# Onde a API do agente esta ouvindo. O front fala com ela DIRETO (sem proxy),
# entao esta URL vai para o bundle -- por isso o prefixo NEXT_PUBLIC_.
# Sem este arquivo copiado para .env.local, o default do codigo ja aponta para
# http://127.0.0.1:8000.
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000
